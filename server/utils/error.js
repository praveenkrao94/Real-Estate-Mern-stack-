// export const errorHandler = (statusCode, message) => {
//     const error = new Error(message); // Pass the message as an argument
//     error.statusCode = statusCode;
//     return error;
//   };
  